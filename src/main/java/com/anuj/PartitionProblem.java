
package com.anuj;

public class PartitionProblem {
    public static void main(String [] args) {

    }

    /**
     * Partition problem is to determine whether a given set can be partitioned
     * into two subsets such that the sum of the elements in both subsets is same.
     */

    public boolean isSubsetSum(int[] arr) {
        //lets try to make sets of all sizes from the array.
        //then we will be able to get to the
        return true;
    }
}
