apply plugin: 'java'
sourceSets {
    main {
        java {
            srcDirs = ['src/main/java']
        }
        resources {
            srcDirs = ['src/main/java', 'src/main/resources']
        }
    }
}

dependencies {
    compile files('libs/algs4.jar')
}
