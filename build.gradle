apply plugin: 'java'
sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
        resources {
            srcDirs = ['src']
        }
    }
}

dependencies{
    compile files('libs/algs4.jar')
}
